# 模型文件目录

此目录用于存放训练好的模型文件。

## 目录结构

```
models/
├── VideoSwin/              # VideoSwin模型
│   ├── VideoSwin.json      # 模型结构文件
│   └── VideoSwin.pdiparams # 模型参数文件
├── YOLO/                   # YOLO轻量级检测模型
└── AudioNet/               # 音频情绪识别模型（规划中）
```

## 模型下载

由于模型文件较大，未包含在Git仓库中。请从以下位置下载：

### VideoSwin模型
- 模型结构: [下载链接]
- 模型参数: [下载链接]
- 大小: 约 XXX MB

### 使用说明

1. 下载模型文件
2. 将文件放置到对应目录
3. 确保文件名正确
4. 在配置文件中指定模型路径

## 注意事项

- 模型文件已通过 `.gitignore` 排除，不会提交到版本控制
- 确保有足够的磁盘空间
- 下载后验证文件完整性
